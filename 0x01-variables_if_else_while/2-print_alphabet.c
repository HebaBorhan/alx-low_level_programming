#include <stdio.h>

/**
 * main - printing alphabet using putchar function
 *
 * Return: 0 for success
 */

int main(void)
{
	char letter = 'a';
	for (letter = 1; letter < 26; letter++)
	{
		putchar(alphabet);

		return = 0
	}
